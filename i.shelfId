const express = require('express'); const app = express(); app.use(express.json()); app.use(express.static('public')); let stockDatabase = []; app.post('/api/stock', (req, res) = const { shelfId, productId, quantity, type } = req.body; let item = stockDatabase.find(i = === shelfId 
